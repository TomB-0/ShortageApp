namespace ShortageApp.Helpers
{
    public class EnumTypes
    {
        public enum RoleType { User, Admin }
        public enum RoomType { MeetingRoom, Kitchen, Bathroom, Other}
        public enum CategoryType { Electronics, Food, Other}

    }
}
